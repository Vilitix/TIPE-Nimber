#include<stdio.h>
#include<stdlib.h>
#include <assert.h>
#include <stdbool.h>

int** board(int x, int y){
    int** res = malloc (sizeof(int*)*y);
    for (int i = 0; i<x;i++){
    int* ligne = malloc(sizeof(int)*x);
    res[i] = ligne 
    }
}
